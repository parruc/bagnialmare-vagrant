"""
Tests can be organized in packages just like this, but every TestCase must be
visible in the first level of the test package thus we need to import
everything with:
    from test_module import *
"""
from basic_tests import *
